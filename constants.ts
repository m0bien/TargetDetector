export const DEFAULT_BASE_SNR_DB = 13; // Default Signal-to-Noise Ratio in dB for a single pulse
export const NUM_POINTS = 500; // Number of points to generate for the PDF plots
export const X_MAX = 10; // Maximum value on the x-axis for the plots